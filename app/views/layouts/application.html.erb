<!DOCTYPE html>
<html>
  <head>
    <title><%= @page ? "#{@page.title} - Dynawiki" : "Dynawiki" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link rel=”icon” href=“/favicon.ico”>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta property="og:url" content="<%= request.url %>" />
    <meta property="og:type" content="Website" />
    <meta property="og:title" content="<%= @page ? "#{@page.title} - Dynawiki" : "Dynawiki" %>" />
    <meta property="og:description" content="Enjoy infinite pages Wiki!" />
    <meta property="og:site_name" content="Dynawiki" />
    <meta property="og:image" content="https://dynawiki.org/ogp.jpg" />
    <meta name="twitter:site" content="Dynawiki" />
    <meta name="twitter:title" content="<%= @page ? "#{@page.title} - Dynawiki" : "Dynawiki" %>" />
    <meta name="twitter:description" content="Enjoy infinite pages Wiki!" />
    <meta name="twitter:image" content="https://dynawiki.org/ogp.jpg"" />
    <meta name="twitter:card" content="summary" />
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= vite_client_tag %>
    <%= vite_javascript_tag 'application' %>
    <!--
      If using a TypeScript entrypoint file:
        vite_typescript_tag 'application'

      If using a .jsx or .tsx entrypoint, add the extension:
        vite_javascript_tag 'application.jsx'

      Visit the guide for more information: https://vite-ruby.netlify.app/guide/rails
    -->
  </head>

  <body class='bg-white font-sans'>
    <header class="py-2 px-5 flex justify-between items-center min-h-[54px]">
      <div class="flex flex-col sm:flex-row  justify-between items-center gap-2 w-full">
        <a href="/<%= params[:lang] %>" class="flex items-center gap-1 mb-[1px] sm:mb-0">
          <img src="/icon.png" width="21" height="21" />

          <span class="font-bold">Dynawiki</span>
        </a>
        <% if request.path_info.include?("wiki") %>
          <%= render partial: "pages/search", locals: { autofocus: false } %>
        <% end %>
      </div>
    </header>
    <main class="p-3">
      <%= yield %>
    </main>
    <% if request.path_info.exclude?("wiki") && request.path_info.exclude?("legal") %>
    <footer class="p-4">
      <div class="flex justify-center gap-1 flex-wrap">
        <div><a href="https://www.patreon.com/dynawiki">Community👭</a></div>
        <div>・</div>
        <div><a href="/en">English</a></div>
        <div>・</div>
        <div><a href="/ja">Japanese</a></div>
        <div>・</div>
        <div><a href="/<%= params[:lang] %>/legal">Legal</a></div>
      </div>
      <div class="text-sm opacity-60 text-center mt-3">
      &copy; 2024 Moeki Kawakami</div>
    </footer>
    <% end %>
  </body>
</html>
